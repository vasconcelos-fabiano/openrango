{
  "scripts": {
    "or": "concurrently -k \"npm run institutional\" \"npm run frontend\" \"npm run proxy\"",
    "institutional": "npm --prefix institutional start -- --host 0.0.0.0 --allowed-hosts --port 4201",
    "frontend": "npm --prefix frontend start -- --host 0.0.0.0 --allowed-hosts --port 4202",
    "proxy": "node dev-server.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "express": "^5.2.1",
    "http-proxy-middleware": "^4.2.0"
  }
}
